"""Промпт, которым из обычного текста получают файл с вопросами.

Контент-менеджер копирует его со страницы загрузки, вставляет в языковую модель
вместе со своим материалом и получает готовый JSON. Правила в тексте — те же,
что проверяет `apps.quizzes.schemas`, а пример подставляется из того же образца,
что лежит рядом на странице: разойтись им негде.
"""

from apps.quizzes.models import MAX_ANSWER_OPTIONS, MIN_ANSWER_OPTIONS
from apps.studio.sample import sample_json


def questions_prompt() -> str:
    """Текст промпта целиком — его копируют одной кнопкой."""
    return f"""Ты готовишь тест для детской образовательной платформы.
Преврати присланный материал в JSON строго по правилам ниже.
В ответе верни только JSON — без пояснений, без markdown и без тройных кавычек.

Правила формата:
1. Корень — объект с единственным ключом "questions", в нём массив вопросов.
2. У вопроса обязательны два ключа: "text" — текст вопроса, "options" — варианты ответа.
3. Ключ "type" необязателен: "single" — верный вариант один, "multiple" — верных несколько.
   Если не указать, подставится "single".
4. Вариантов ответа от {MIN_ANSWER_OPTIONS} до {MAX_ANSWER_OPTIONS}. У каждого есть "text"
   длиной не больше 500 символов.
5. Верный вариант помечается "is_correct": true. У неверных этот ключ можно не писать.
6. При "single" верный ровно один. Если верных больше одного — ставь "type": "multiple".
7. Других ключей быть не должно: на лишний ключ загрузка откажет.
8. Порядок вопросов — это порядок в массиве, нумеровать их не нужно.
9. Язык — русский. Формулировки короткие, понятные ребёнку, без двусмысленных вариантов.

Пример готового файла:
{sample_json()}

Материал, из которого нужно сделать вопросы:
<<< вставьте сюда свой текст >>>
"""
